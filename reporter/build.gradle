dependencies {
    implementation project(':collector')
    implementation project(':cost-engine')
}

jacocoTestCoverageVerification {
    violationRules {
        rule {
            limit {
                minimum = 0.99
            }
        }
    }
}
